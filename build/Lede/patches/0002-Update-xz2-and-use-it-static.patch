--- a/src/bootstrap/src/core/config/config.rs
+++ b/src/bootstrap/src/core/config/config.rs
@@ fn fn download_ci_llvm(&self, config: &TomlConfig) -> bool {
-        if download_ci_llvm && self.on_ci {
-            panic!("`llvm.download-ci-llvm` cannot be set to `true` on CI. Use `if-unchanged` instead.");
-        }
+        // Override the panic on CI
+        if download_ci_llvm && self.on_ci {
+            eprintln!("Warning: forced llvm.download-ci-llvm = false on CI to avoid panic");
+            return false;
+        }
